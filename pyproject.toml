[project]
name = "project-5-drift-triage"
version = "0.1.0"
description = "Drift Triage Co-Pilot monorepo"
requires-python = ">=3.11,<3.12"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "model_service",
    "agent",
    "worker",
    "dashboard"
]

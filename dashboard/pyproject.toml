[project]
name = "drift-dashboard"
version = "0.1.0"
description = "Streamlit dashboard for Drift Triage Co-pilot"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.35.0",
    "requests>=2.32.0",
    "pandas>=2.2.0",
    "python-dotenv>=1.0.1",
]

[tool.uv]
package = false